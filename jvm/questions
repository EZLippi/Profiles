List、Set、Map是否都继承自Collection接口,MAP实现类，HashMap 1.7,1.8,ConcurrentHashMap
JVM内存布局，object对象内存布局，一个空Object对象的占多大空间？
TimerTask和SchedualThreadPoolExecutor区别
mysql锁加在什么位置
 myisam和innodb的区别
 阐述事务的隔离级别和传播属性,mysql的默认隔离级别
 SQL注入的原理
 什么场景下会触发一次GC
 你怎么理解cookie和session，有哪些不同点
 OutofMemory的可能原因有哪些
 Spring如何实现多数据源
 Tomcat ClassLoader机制
 API和SPI区别
 列举3个以上的RuntimeException
 <? extends T>和<? super T>
  List<? super T> doSomeThing(List<? extends T>){}
 
Integer a = new Integer(1), b = new Integer(1);
      Integer c = 1, d = 1;
      System.out.println(a==b);//false
      System.out.println(c==d); //true
http协议格式，get和post的区别

class Transationc{
a(){
b();}
@Trasaction
b(){};
}

https握手机制
